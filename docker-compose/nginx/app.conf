#
# HTTP → HTTPS + ACME challenge
#
server {
    listen 80;
    server_name dev-camenu-qr.dpathemes.org;

    # serve the ACME challenge (important for renewal)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # catch-all for everything else
    location / {
        # redirect browsers to HTTPS
        return 301 https://$host$request_uri;
    }
}

#
# HTTPS – main application
#
# server {
#     listen 443 ssl;
#     server_name ev-camenu-qr.dpathemes.org;

#     ssl_certificate     /etc/letsencrypt/live/dev-content.ibfnxt.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dev-content.ibfnxt.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;

#     root   /var/www/public;
#     index  index.php index.html;

#     # Security: upgrade insecure requests (helps mitigate mixed-content warnings)
#     add_header Content-Security-Policy "upgrade-insecure-requests" always;

#     # CORS handling for API endpoints

#     # Whitelist origins using regex and explicit localhost entries.
#     # Only origins that match these checks will be echoed back.
#     set $allowed_origin "";
#     if ($http_origin ~* "^https?://([a-z0-9-]+\.)*ibfnxt\.com(:[0-9]+)?$") {
#         set $allowed_origin $http_origin;
#     }
#     if ($http_origin ~* "^https?://([a-z0-9-]+\.)*ibf\.com\.kh(:[0-9]+)?$") {
#         set $allowed_origin $http_origin;
#     }
#     if ($http_origin = "http://localhost:3000") {
#         set $allowed_origin $http_origin;
#     }
#     if ($http_origin = "http://127.0.0.1:3000") {
#         set $allowed_origin $http_origin;
#     }

#     location /api/ {
#         # Handle CORS preflight
#         if ($request_method = 'OPTIONS') {
#             add_header 'Access-Control-Allow-Origin' "$allowed_origin" always;
#             add_header 'Vary' 'Origin' always;
#             add_header 'Access-Control-Allow-Credentials' 'true' always;
#             add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE' always;
#             add_header 'Access-Control-Allow-Headers' 'Authorization,Origin, X-Requested-With, Content-Type, Accept, X-XSRF-TOKEN' always;
#             add_header 'Access-Control-Max-Age' 1728000;
#             add_header Content-Type 'text/plain; charset=UTF-8';
#             add_header Content-Length 0;
#             return 204;
#         }

#         # Response headers for normal requests
#         add_header 'Access-Control-Allow-Origin' "$allowed_origin" always;
#         add_header 'Vary' 'Origin' always;
#         add_header 'Access-Control-Allow-Credentials' 'true' always;
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE' always;
#         add_header 'Access-Control-Allow-Headers' 'Authorization,Origin, X-Requested-With, Content-Type, Accept, X-XSRF-TOKEN' always;

#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         fastcgi_pass   app:9000;
#         fastcgi_index  index.php;
#         include        fastcgi_params;
#         fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }

#     location ~ /\.ht {
#         deny all;
#     }
# }
